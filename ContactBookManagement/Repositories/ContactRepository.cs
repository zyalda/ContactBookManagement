using System;

namespace ContactBookManagement.Repositories
{
    public class ContactRepository
    {
        //CRUD principle....
        public void AddContact(Contact contact)
        {
            //if (ValidationMethod(contact))
            //    //Update text file
            //else
            Console.WriteLine("Add contact failed, try again.");
        }
    }
}
